# Формат тестового файла:
# Каждая строка содержит тест в формате: (функция(аргументы), ожидаемый_результат)
# Скрытые тесты помечаются символом # в начале строки

(fun(5), 10)
#(fun(8), 16)
(fun(-3), -6)
(fun(0), 0) 